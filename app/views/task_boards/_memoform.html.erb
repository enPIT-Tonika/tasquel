<%= form_for @memo do |m| %>
    	<p>
    		<%= m.text_area :taskmemo, size: "30x3" %>
    		<% if @memo.errors.any? %>
    		<%= @memo.errors.message[:taskmemo][0] %>
    		<% end %>
    	</p>
    	<p>
    		<%= m.submit '更新'%>
    	</p>
<% end %>


